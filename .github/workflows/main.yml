name: Local-Deploy-Check
on: [push]

jobs:
  deploy:
    # CHANGE THIS LINE from 'ubuntu-latest' to 'self-hosted'
    runs-on: self-hosted
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Run Ansible Ping
        run: |
          cd ansible
          ansible all -i inventory -m ping
